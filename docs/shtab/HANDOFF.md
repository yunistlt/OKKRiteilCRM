# Передача дел: Штаб владельца, на чём остановились

Файл написан облачной сессией Claude Code для сессии на Маке. Причина переезда:
код ЦехУспеха лежит в `/Users/andreiterenkov/ZehUspeh AI`, а облачный контейнер
файловую систему Мака не видит и увидеть не может. Дальше нужен один рабочий
стол, на котором видны сразу оба проекта.

Читается один раз в начале новой сессии. Дальше опорные документы —
[OVERVIEW.md](OVERVIEW.md), [TAMARA.md](TAMARA.md), [DUTY_API.md](DUTY_API.md).

## Правило, которое важнее остального

**Папка `/Users/andreiterenkov/ZehUspeh AI` — только для чтения. Ничего не
менять, не создавать, не удалять.**

ЦехУспех — боевая система, на которой стоит цех, и у неё **нет истории версий**:
ни GitHub, ни GitLab. Деплой — rsync на VPS Timeweb, откат — только через ручные
`backups/` и `.bak-*`. Ошибка там не откатывается коммитом, она останавливает
производство.

## Что такое Штаб

Командный пункт владельца по методологии «Альянс Стратег» внутри OKKRiteilCRM.
Цепочка методологии: минусы → приоритетная область → ситуация → «почему» →
краткосрочная цель из двух частей → карта ресурсов → стратегия → **логические
блоки → программы → задачи пяти типов** → проекты.

Живёт в `app/shtab`, `app/api/shtab/*`, `lib/shtab/*`, `docs/shtab/*`.

### Тамара и три её слоя

Тамара — консультант владельца. Устроена так, что модель не может ни соврать
числом, ни соврать методологией:

| Слой | Где | Смысл |
|---|---|---|
| Проверки методологии | `lib/shtab/program-checks.ts` | Детерминированный код, не модель. Один руководитель, главная задача результатом, есть жизненно важные, у производственной задачи либо число, либо пропуск с названным источником |
| Факты о компании | `lib/shtab/tamara-tools.ts` | Только через инструменты. **Модель никогда не получает SQL** — только параметры |
| Управленческие знания | `lib/shtab/kb-content.ts` (58 статей) + `match_shtab_kb` | Векторный поиск по методичке |
| Выясненные факты | `lib/shtab/memory.ts`, `shtab_memory` + `shtab_kb` тип `company` | Не знает — спрашивает владельца и записывает ответ дословно |

Инструментов сейчас семь: `shtab_state`, `shtab_history`, `money_in`,
`shtab_razbor_detail`, `shtab_programs` читают базу Штаба, `shtab_zapomnit` и
`shtab_fakt` работают с выясненными фактами. **Из ЦехУспеха Тамара не видит
ничего.**

Про память подробно — в [TAMARA.md](TAMARA.md), раздел «Не знает — спрашивает».
Коротко: подробность с дословным ответом владельца ложится в `shtab_kb` (тип
`company`), а в `shtab_memory` идёт короткая отметка со slug, и она всегда в
системном сообщении. Без второго слоя векторный поиск не подсказал бы, что факт
вообще существует, и Тамара спросила бы то же самое второй раз — после чего
владелец перестал бы отвечать.

Образец программы (`lib/shtab/program-example.ts`) намеренно **не** положен в
RAG: нарезка на куски научила бы половине формы.

## Состояние веток и миграций

Работа идёт в ветке `claude/alliance-strategy-management-system-dkpcf9`, она
впереди `main` и запушена целиком. Что именно в ней — `git log --oneline
origin/main..HEAD`: перечислять здесь бессмысленно, список устареет на следующем
коммите.

Семь миграций, **в бою применена одна**:

| Файл | В бою |
|---|---|
| `20260825_shtab_owner_hq.sql` | да |
| `20260826_shtab_tamara.sql` | нет |
| `20260827_shtab_programs.sql` | нет |
| `20260827_shtab_google.sql` | нет |
| `20260828_shtab_duty.sql` | нет |
| `20260831_shtab_memory.sql` | нет |
| `20260831_shtab_tamara_smarter.sql` | нет |

Тесты: 372 в 28 файлах, из них десять по Штабу (`tests/shtab-*.test.ts`).

## Слой внешних баз: что уже проверено, не переоткрывать

`lib/shtab/external/` — `client.ts`, `guard.ts`, `postgres.ts`, `mysql.ts`.
Движок определяется схемой строки подключения (`mysql://` против `postgres://`),
а не жёсткой привязкой «ЦУ → MySQL»: базу можно перевезти, не трогая код.
Ключи окружения: `SHTAB_DB_TSEH_URL`, `SHTAB_DB_KB_URL`, `SHTAB_DB_MARKETING_URL`.

Три рубежа защиты от записи, каждый испытан на живых PostgreSQL 16 и MySQL 8.0.46:

1. роль в базе (`tamara_ro`, только `SELECT` плюс `EXECUTE` поимённо);
2. текстовая проверка запроса — `assertReadOnlyQuery` в `guard.ts`;
3. read-only транзакция, всегда с `ROLLBACK`.

Четыре факта, добытые опытом, а не рассуждением:

- **`charset: 'utf8mb4'` в драйвере MySQL обязателен.** На колонках `cp1251`
  (ЦУ — Delphi, там это обычное дело) соединение в `latin1` вернуло `? ????????`
  и фильтр `WHERE NameStatus = 'В производстве'` дал **0 из 2 строк без единой
  ошибки**. Худший вид поломки: запрос отработал, а Тамара говорит владельцу, что
  заказов в производстве нет. Явная настройка в коде перебивает `?charset=` в
  строке подключения.
- **`EXECUTE` безопасен только внутри read-only транзакции.** Функция с
  `SQL SECURITY DEFINER`, пишущая в таблицу, внутри `START TRANSACTION READ ONLY`
  дала `ERROR 1792 (25006)`; без транзакции запись прошла. Значит `EXECUTE`
  держится третьим рубежом, и правило «к внешним базам только через
  `queryExternal`» — не стиль, а условие безопасности.
- **Грант на функции — поимённо, не на всю базу.** На том же сервере MySQL живут
  базы других тенантов, а DEFINER-функция читает с правами своего владельца.
- **В Postgres внешние ключи брать из `pg_catalog`.**
  `information_schema.constraint_column_usage` показывает только таблицы, которыми
  роль владеет, и read-only роли молча отдаёт пустоту: все связи исчезают без
  ошибки. Исправлено в `scripts/shtab_discover.ts`.

Оговорка, которая честно печатается в снимке схемы: в MySQL нельзя определить,
что `GRANT SELECT` выдан не на все таблицы — `information_schema` фильтруется
правами с обеих сторон, и недостающая таблица выглядит просто отсутствующей.
Показывать в этом случае ноль нельзя, ноль читался бы как «всё выдано».

## Чего нет

- **Инструментов `tseh_*` не существует.** Труба построена, кранов на конце нет.
- **Схема ЦехУспеха до облачной сессии не дошла.** Соседняя сессия сняла её с
  боевой базы ЗМК и собрала файл `для-ОКК-Тамара-доступ-к-ЦехУспеху.md` с готовым
  SQL на пять денежных запросов — записать в этот репозиторий ей не дал
  классификатор, в чат он тоже не попал. Если файл сохранился на Маке — найти и
  положить как `docs/shtab/schemas/tseh.md`.

Из всей базы ЦУ облачной сессии достоверно известны три имени: `ListBalanses`
(ежедневные снимки 18 финансовых показателей с 29.11.2020, ~68 тыс. строк),
`SalaryOrder` и `CostOrderExt` (расчётные функции, без них не считается прибыль).

## Стратегия и программы: откуда взялся цеховой контур

Стратегия владельца — **«наладить такт выпуска готовой продукции 5 млн в неделю»**.
Под неё написаны пять программ, опубликованы страницей:

- программы — https://claude.ai/code/artifact/8d08a294-b47f-4e8c-8a28-854a39f7921a
- памятка по состоянию дел — https://claude.ai/code/artifact/99163695-16f8-465d-9e02-01451da56680

В их производственных задачах стоит **12 пропусков**. Пропуск законен только с
названным способом замера; сейчас способ у всех один — пойти и померить руками:

| Программа | Показатель | Единица |
|---|---|---|
| 0. Замер такта | требуемый такт | ед./смену |
| 0. Замер такта | пропускная способность узкого места | ед./смену |
| 1. Покраска | пропускная способность покраски | ед./смену |
| 1. Покраска | простои покраски по отказам | ч/мес |
| 2. Заготовки | вопросов «что это и куда дальше» за смену | шт |
| 2. Заготовки | межоперационное пролёживание | ч |
| 3. Дисциплина | нарушений за неделю | шт |
| 3. Дисциплина | потери рабочего времени из-за отсутствия на месте | % смены |
| 4. Штат | текучесть в первые три месяца | % |
| 4. Штат | обращений по объявлению | чел./мес |
| 3. Дисциплина | глубина архива | дней |

Владелец просит, чтобы эти числа отдавал ЦехУспех, а не ручной замер. Тогда срыв
такта виден в тот же день, а не на разборе через месяц.

Закроются не все. Первые четыре строки таблицы — кандидаты на закрытие фактом из
истории операций; строки про нарушения, потери времени и вопросы рабочих не
закроются ничем (см. третий уровень ниже); кадровые зависят от того, есть ли в ЦУ
даты приёма и увольнения. Разложить окончательно сможет только локальная сессия,
увидев код.

## Главный вопрос к коду ЦехУспеха

Установлено со слов владельца: **время операции высчитывается по истории, и
история накоплена за несколько лет.** Значит худший исход отпал — время в системе
есть, и не одной точкой, а длинным рядом. Проверять «есть ли время» не нужно.

Вопрос теперь другой, и он опаснее прежнего.

### Норма или факт

«Высчитывается по истории» читается двумя способами, и они отвечают на разные
вопросы:

**Норма** — сколько операция *должна* занимать, выведенное из накопленной
статистики. Отвечает: сколько влезет в смену по расчёту.

**Факт** — когда операцию *реально* отметили выполненной. Отвечает: сколько
прошло на самом деле.

Подменять одно другим нельзя, и это не придирка. Расчётная пропускная
способность покраски по нормам скажет «должна пропускать 40 в смену». Факт
скажет «пропускает 26». **Разница между этими числами и есть то, ради чего
написана Программа 1.** Отдать норму, назвав её фактом, — значит заставить
программу проверять саму себя: она всегда сойдётся, а цех как стоял, так и стоит.

| Показатель | Из нормы | Из факта |
|---|---|---|
| Такт выпуска, ед./смену | нет | да |
| Пропускная способность передела | расчётная, как первое приближение | да, и это настоящая |
| Узкое место | по расчёту загрузки | по факту, точнее |
| Межоперационное пролёживание | **нельзя** | только факт, и нужна отметка времени, а не одна дата |
| Простои по отказам | нет | нет: причины простоя в ERP не хранятся |

Поэтому первым делом надо установить не «есть ли время», а **что именно лежит в
истории построчно** — событие отметки или уже посчитанный агрегат.

**Решение владельца: опираемся на факт, не на норму.** Это правило, а не
предпочтение:

- ни один инструмент `tseh_*` не отдаёт норму под видом достигнутого;
- норма показывается только рядом с фактом и подписанная как норма — в этом виде
  она полезна: расхождение факта с нормой само по себе показатель;
- если по какому-то показателю в истории есть только норма, инструмент честно
  говорит, что факта нет, и пропуск в программе остаётся пропуском. Пропуск
  владелец видит. Норму, названную фактом, он принимает за правду — и это хуже.

### Как ставится отметка: техкарта закрывается самим рабочим

Со слов владельца: **отметку ставит сам рабочий в техкарте, и держать её
незакрытой он не может — иначе партия не перейдёт на следующий передел.**

Это лучшее, что можно было услышать, и вот почему. Закрытие обеспечено
устройством процесса, а не дисциплиной. Прежнее опасение — «отметки проставляют
пачкой в конце недели, и тогда пролёживание из них не считается» — снято: партия
физически не двинется, пока техкарта открыта. Значит покрытие близко к полному, и
отметка привязана к настоящему событию передачи, а не к моменту, когда мастер сел
за компьютер.

Из этого сразу следует четыре вещи:

- **такт** — выпуск считается по закрытию техкарты на последнем переделе маршрута;
- **пропускная способность передела** — число закрытий на нём за смену;
- **узкое место** — передел, перед которым интервалы между закрытиями
  систематически больше: там очередь, и её видно без единого замера в цехе;
- **незавершёнка в моменте** — открытые техкарты и их возраст. Это не история, а
  оперативный сигнал: «на покраске три техкарты открыты дольше суток» Тамара может
  сказать сегодня.

#### Что осталось выяснить: есть ли отметка начала

Главная развилка, которая теперь решает всё:

**Техкарта фиксирует только закрытие — или ещё и взятие партии в работу?**

- Есть обе отметки → пролёживание считается **прямо**: разница между закрытием на
  переделе N и взятием в работу на N+1. Ожидание отделено от работы.
- Есть только закрытие → интервал между соседними закрытиями = **ожидание плюс
  работа, слитые вместе**, и напрямую их не разделить. Тогда пролёживание ≈
  интервал минус норма времени операции.

Второй случай — единственное место во всей затее, где норма нужна по делу: **не
как ответ, а как вычитаемое.** И подписывать такое число надо соответственно:
«оценка, из интервала за вычетом нормы», а не «пролёживание».

#### Остальные вопросы по техкарте

1. Одна техкарта на партию или на деталь? От этого зависит единица счёта во всех
   цеховых числах.
2. «Не перейдёт на следующий передел» — это программная блокировка или
   организационное правило? Если программная, в базе есть её след: статус, ссылка
   на предыдущий передел, проверка при открытии. Если организационное — покрытие
   уже не полное, и это надо знать.
3. Можно ли отменить или переоткрыть закрытую техкарту? Если да — остаётся ли
   след? Без следа история переписывается задним числом, и вчерашнее число
   перестаёт сходиться с сегодняшним.
4. Брак и переделка: заводится новая техкарта или переоткрывается старая? Наивный
   подсчёт выпуска во втором случае завысит его.
5. Что именно записывается при закрытии — дата или дата со временем? Для такта
   хватит даты, для пролёживания в часах нужно время.

### Многолетний ряд — это XmR, а не просто «много данных»

В Штабе уже есть `lib/shtab/xmr.ts`: карта индивидуальных значений Шухарта,
границы `cl ± 2.66·mR̄`, `MIN_POINTS = 12`, серия из `RUN_LENGTH = 8` точек по
одну сторону считается сигналом. Функция `xmr(data: readonly number[])` берёт
голый ряд чисел — подключить к ней цеховой ряд стоит ничего.

Сейчас она работает по недельным статистикам Штаба, которых накоплено мало.
Несколько лет цеховой истории — это сотни недельных точек по каждому переделу, то
есть границы, посчитанные из самого ряда, а не назначенные пальцем. Тогда
«покраска просела» перестаёт быть впечатлением: видно, вышла точка за границу или
это обычное колебание, которое было и в прошлом году.

**Своего способа отличать сигнал от шума не изобретать** — он уже написан и
покрыт тестами (`tests/shtab-xmr.test.ts`).

### Что смотреть в коде ЦУ

**Заказы и выпуск.** Таблицы заказов и позиций; поля дат (создан, запущен, план,
факт закрытия) и какие из них реально заполняются; количество в физических
единицах или только деньги; справочник статусов — все коды и русские названия,
какой означает «выпущено».

**Техкарта и её история — главное.** Кто ставит отметку, уже известно: сам
рабочий, и закрытие обязательно для перехода партии дальше. Смотреть в таком
порядке:

1. Таблица техкарт и таблица их истории: имена, ключевые поля, связь с заказом и
   переделом.
2. **Есть ли отметка взятия в работу или только закрытия?** Главная развилка —
   см. раздел про техкарту выше. От неё зависит, считается пролёживание прямо или
   через вычитание нормы.
3. Гранулярность отметки: дата или дата со временем.
4. Единица: техкарта на партию или на деталь.
5. Привязка: к заказу, к переделу или участку, к исполнителю.
6. Механизм блокировки перехода — программный или организационный, и какой след
   он оставляет в базе.
7. Отмена и переоткрытие закрытой техкарты: возможны ли, остаётся ли след.
8. Брак и переделка: новая техкарта или переоткрытие старой.
9. По какому правилу и окну выводится норма времени: среднее, медиана,
   перцентиль. Нужно не чтобы её отдавать, а чтобы не спутать с фактом — и чтобы
   корректно вычитать, если отметки начала нет.
10. Глубина истории и объём строк.

**Операции и маршрут.** Таблица операций/техпроцесса; есть ли у операции участок
или передел, исполнитель, норма времени, расценка, плановые даты; через какой
экран операция отмечается выполненной; тела `SalaryOrder` и `CostOrderExt`
целиком; справочник участков или рабочих центров.

**Люди.** Справочник сотрудников: дата приёма, дата увольнения, участок,
должность. Табель или учёт рабочего времени.

**Оборудование.** Ремонты, ТО, простои. Скорее всего ничего — тогда так и
записать, это нормальный ответ.

**Общая картина.** Полный список таблиц с порядком числа строк и строкой «что
это»; какие живые, какие мёртвые от старых версий; есть ли у ЦУ HTTP API и что за
стек у контейнера `zeh-app`; мультитенантность — одна база на тенанта или общая с
колонкой тенанта, и как называется база ЗМК.

Правило ответа: чего не нашёл — писать «нет», а не молчать. Пропуск читается как
«есть, просто не написал», и это хуже прямого «нет».

## Три уровня, на которые ляжет ответ

1. **Есть в ЦУ фактом — взять.** Выпуск, заказы, события по операциям, люди.
2. **Выводится из факта — посчитать.** Такт, пропускная способность передела,
   узкое место, пролёживание, укомплектованность. Плюс XmR по каждому ряду:
   многолетняя история даёт границы, посчитанные из самого ряда.
3. **Нет и не появится в ЦУ.** Простои по отказам, нарушения дисциплины, вопросы
   рабочих «что это и куда». Ни одна ERP не хранит причину простоя и факт
   нарушения. **Обещать третий уровень из ЦУ нельзя.**

   Но источник у части из них всё-таки есть, и не ручной: **камеры**. Сервер в
   цехе видит, что станок не двигался, и видит очередь перед переделом. Причину
   простоя он не знает — её вписывает человек. То есть пустое место превращается
   в число с ручной разметкой причины. Техническая часть — в
   [CAMERAS.md](CAMERAS.md). Оговорка: на странице программ наблюдение живёт
   внутри Программы 3 и служит только дисциплине; простой оборудования и очередь
   перед переделом туда не вошли — они не про дисциплину, и их пропуски в
   Программах 0 и 1 по-прежнему закрываются ручным замером.

Третий уровень после уточнения про историю **сжался, но не исчез**. Утверждение
«цеховой контур может не достаться вовсе» больше не верно — время в системе есть.
Но и обратное неверно: несколько лет истории не превращают ЦехУспех в источник
того, чего он никогда не записывал.

## Что делать дальше в локальной сессии

1. Ответить на главный вопрос — есть ли в техкарте отметка взятия в работу или
   только закрытия — со ссылкой на файл и строку, а не пересказом.
2. Разложить 12 показателей по трём уровням.
3. `docs/shtab/schemas/tseh.md` — снимок схемы (структура, без данных).
4. `lib/shtab/tseh-tools.ts` — инструменты поверх `queryExternal('tseh', …)`.
   SQL константами в коде. Денежные — по готовому SQL соседней сессии, цеховые —
   по закрытиям техкарт. Отдельно от исторических рядов сделать оперативный
   инструмент по открытым техкартам и их возрасту: это незавершёнка в моменте,
   и она отвечает на вопрос «где стоим прямо сейчас», а не «как было в среднем».
5. Подключить их в `lib/shtab/tamara-tools.ts` рядом с пятью существующими.
   Цеховые ряды прогонять через `xmr` из `lib/shtab/xmr.ts`, свой способ отличать
   сигнал от шума не изобретать.
6. Мягкая деградация по образцу `utils/openai.ts`: нет `SHTAB_DB_TSEH_URL` или
   база недоступна — внятное «данных цеха сейчас нет», а не падение чата.
7. `tests/shtab-tseh-tools.test.ts`: разбор ответа, поведение без переменной
   окружения, прогон каждого SQL через `assertReadOnlyQuery`.
8. Переписать производственные задачи в программах: пропуски, которые закрывает
   ЦУ, получают источником инструмент вместо ручного замера.

**Правило приёмки цехового числа:** каждое сверяется с ручным замером по одному
участку за одну смену. Расходится — инструмент не выпускается. Неверное число
хуже пропуска: пропуск владелец видит, а неверное число он принимает за правду.

Сверять именно с ручным замером, а не с тем, что показывает сам ЦехУспех на
экране. Совпадение с экраном доказывает только, что SQL повторил чужую формулу —
включая её ошибки. Ручной замер проверяет, что число описывает цех.

## Что остаётся на владельце

- Учётка `tamara_ro` на боевой MySQL — готовый SQL в [TAMARA.md](TAMARA.md), шаг 1.
  `SELECT` на базу, `EXECUTE` поимённо на `SalaryOrder` и `CostOrderExt`, лимиты
  подключений.
- `SHTAB_DB_TSEH_URL` = `mysql://tamara_ro:…@host:3306/db` в переменных окружения
  Vercel и в `.env.local`. **Не в чат и не в репозиторий.**
- Проверить, достижим ли контейнер `zeh-mysql` снаружи VPS: `nc -z <хост VPS> 3306`
  с любой посторонней машины. Vercel ходит с меняющихся адресов из интернета,
  SSH-туннель из serverless не поднять. Закрыт — надо публиковать порт и открывать
  его в firewall.
- Слить ветку и применить шесть миграций.
- `npm run shtab:seed-kb` — залить 58 статей базы знаний.
- Google Cloud проект для календаря; `SHTAB_TOKEN_KEY` (≥16 символов)
  **менять нельзя после подключения календаря** — им зашифрован refresh-токен.
- Заполнить `external_uid` на постах и назначить программы постам — иначе
  консультант ЦехУспеха не поймёт, кому какие задачи показывать
  ([DUTY_API.md](DUTY_API.md)).

## Открытые концы

- Соседняя сессия должна сказать, какой `uid` она будет присылать в `/api/duty/*`.
- Движки баз КБ и маркетинга неизвестны — слой это переживёт, определится по схеме
  строки подключения.
- PR не открыт: владелец не просил.
