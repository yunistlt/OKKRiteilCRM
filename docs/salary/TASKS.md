# Зарплата ОП — задачи (бэклог)

Открытые задачи и долг по модулю. Связано с [OVERVIEW.md](OVERVIEW.md) (§13 «Долг/TODO») и [PLAN.md](PLAN.md).
Статусы: ⬜ не начато · 🟡 в работе · ✅ сделано. При закрытии задачи — отметить и при необходимости обновить OVERVIEW.

## Готово (для контекста)
- ✅ Движок на бонус-блоках + golden-тест тождества прежней формуле.
- ✅ Схемы (роли) «Продавец»/«Оператор», реестр ОП (`salary_manager_comp`), фикс лишних людей (Лариса-логист вне расчёта).
- ✅ Таблица планов `salary_plan` + API/UI планов.
- ✅ Каталог блоков: 7 ядровых + план/объём/SPIFF/качество; редактор параметров полями (не сырой JSON).
- ✅ API `blocks`/`schemes`/`plans`; вкладки «Настройки мотивации» (Схемы/Реестр/Планы/Базовые).
- ✅ Отчёт по менеджеру с кликабельными номерами заказов → карточка в ОКК; доступ роли manager к карточке.
- ✅ Бэкдейт конфига (расчёт май/июнь), устойчивый источник засчитанных заказов.
- ✅ UI приведён к голдам (плоско, 0px радиус, без теней, плотно).

## Открытые задачи

### Таблицы — полное соответствие GOLD_UI_TABLES (главная таблица «Зарплата ОП»)
- ⬜ Sticky-заголовок при скролле.
- ⬜ Зебра строк, hover.
- ⬜ Sticky первая колонка (менеджер) при горизонтальном скролле.
- ⬜ Zoom таблицы (`− NN% +`, хоткеи Ctrl±/0, `zoom` на `<table>`, localStorage `okk_table_zoom`).
- ⬜ Шестерёнка: видимость/порядок колонок; колонки из конфига, а не хардкод.
- ⬜ Фильтры (клиент/сервер), пресеты-вкладки, индикация активного фильтра.
- ⬜ Bulk-actions с чекбоксами; quick-view сайдбар по клику строки.
- ⬜ Пагинация 20/50/100.
- ⬜ Цветовая шкала оценок (90+/70+/50+/<50) и сокращение сумм «к».

### Расчёт / данные
- ⬜ `salary_adjustment`: реализовать логику и UI корректировок для закрытых периодов (таблица есть, кода нет).
- ⬜ Конв-бонус: `breakdown.conversionEligible` берётся из глобального конфига; если схема задаёт свой
  `minZayavki` — подпись в отчёте может расходиться с фактом блока (сам расчёт корректен). Считать eligible из блока.
- ⬜ Блок `margin`: данные partial (purchasePrice не у всех заказов) — решить, как считать/показывать достоверность.
- ⬜ Экспорт Excel: добавить колонки по вкладу блоков (`breakdown.blockContributions`), сохранив legacy для закрытых.
- ⬜ Отчёт по менеджеру: рендерить `blockContributions` с методикой и бейджем заполненности данных (dataFill).

### Модель / расширения
- ⬜ Пер-менеджерные оверрайды параметров блока (например индивидуальный оклад поверх схемы) — точка расширения
  `salary_manager_comp.overrides`.
- ⬜ Новые блоки (план/объём/SPIFF/качество) — операционно перетащить в нужные схемы в конструкторе (это настройка, не код).
- ⬜ Штраф за отмены (`order_cancel`) — добавить сбор метрики отмен в `metrics.ts` и блок-штраф.

### Документация
- ⬜ Держать `OVERVIEW.md` в актуальном состоянии при изменениях кода.
- ⬜ (Опц.) диаграмма потока данных картинкой.
