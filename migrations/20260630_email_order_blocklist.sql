-- Катерина: список исключений на СОЗДАНИЕ ЗАКАЗОВ.
-- Адреса/домены отправителей, по письмам от которых НЕ нужно заводить заказ
-- (например, тендерные робо-рассылки, которые шлют все тендеры подряд независимо от категории).
-- Письмо при этом разбирается как обычно (классифицируется, может переслаться в отдел) —
-- подавляется только создание заказа. Список в БД (zero-hardcode), правится в интерфейсе.
ALTER TABLE public.email_intake_config
    ADD COLUMN IF NOT EXISTS order_blocklist TEXT[] NOT NULL DEFAULT '{}';
